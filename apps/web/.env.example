# ATS-Ninja-V2 web environment (copy to .env.local; never commit real values)

# Base URL of the FastAPI backend used by browser calls.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Legacy alias remains supported for existing local setups:
# NEXT_PUBLIC_API_URL=http://localhost:8000
