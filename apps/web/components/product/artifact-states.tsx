import { Ban, CircleMinus } from "lucide-react";
import { Card, StatusLabel } from "@/components/ui/primitives";
import { notRequestedPresentation, withheldPresentation } from "@/lib/status";

export function NotRequestedArtifact({ title }: { title: string }) { return <Card className="mx-auto max-w-[680px] text-center"><CircleMinus aria-hidden="true" className="mx-auto size-10 text-ink-muted" /><div className="mt-4"><StatusLabel presentation={notRequestedPresentation} /></div><h2 className="mt-3 text-lg font-semibold">{title} was not requested</h2><p className="mt-2 text-sm text-ink-secondary">This is an intentional absence from the persisted selection, not a generation failure.</p></Card>; }
export function WithheldArtifact({ title, reason }: { title: string; reason?: string }) { return <Card className="mx-auto max-w-[680px] border-danger-border bg-danger-bg text-center"><div className="mx-auto grid size-14 place-items-center rounded-lg border border-danger-border bg-surface text-danger"><Ban aria-hidden="true" className="size-7" /></div><div className="mt-4"><StatusLabel presentation={withheldPresentation} /></div><h2 className="mt-3 text-lg font-semibold text-danger">{title} was withheld</h2><p className="mt-2 text-sm text-ink-secondary">{reason || "The backend rejected this artifact because a truth-critical or structural validation requirement was not satisfied."}</p></Card>; }
